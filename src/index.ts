/**
 * Main entry point for the Azure Functions application.
 *
 * Importing these modules registers each function with the Azure Functions
 * v4 programming model via the `app.*` calls within each file.
 */
import './functions/addressValidation';
import './functions/incomeClassification';
import './functions/legislativeMapping';
